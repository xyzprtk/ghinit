# repox

`repox` is a Python CLI that creates GitHub repositories and initializes the
current directory in one guided command.

## Install

```bash
pip install -e .
```

## Usage

```bash
repo my-app --private --template flask
```
